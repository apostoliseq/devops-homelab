FROM postgres:16-alpine

# Init scripts placed in /docker-entrypoint-initdb.d/ are run automatically
# the first time the container starts (when the data volume is empty).
# Add .sql or .sh files here in future phases to pre-create schemas or seed data.
